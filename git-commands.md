# Git commands (used on this repository)


### Branch

`git push --set-upstream origin <branch_name>`

 This command is used to publish your local branch to the remote repository named 'origin' and establish a tracking relationship between them. The shorthand for `--set-upstream` is `-u`

 
